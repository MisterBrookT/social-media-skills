# AGENTS.md

- 用户可见内容默认中文。
- skill 保持 harness-neutral：不要写 Claude-only `@import`。
- 结构参考 `superpowers`：`skills/` 下平铺多个可组合 skill，每个 skill 目录一个 `SKILL.md`。
- `social-media` 是总入口；平台和任务用独立 skill，不塞成巨型总文件。
- 首版不做平台自动发布；内容、封面、图文、转译先闭环。
- 新 prior art 进入 `skills/social-media/references/prior-art.md`，同时同步 Obsidian source note。
